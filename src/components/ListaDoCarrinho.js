import React from "react"
import styled from "styled-components"
import CardCamiseta from "./CardCamiseta"

localStorage.getItem("carrinho")

export default function ListaDoCarrinho(props) {
    return (
        <div>
        list.
        </div>
)
}